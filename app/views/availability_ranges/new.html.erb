<% if @availability_range.errors.any? %>
     <div id="error_explanation">
       <h2><%= pluralize(@availability_range.errors.count, "Error") %> prevented this from being created:</h2>

       <ul>
         <% @availability_range.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
         <% end %>
       </ul>
     </div>
   <% end %>

<h1>AvailabilityRanges</h1>
<% weekdays = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'] %>
<% x = 0 %>
<% 7.times do %>
  <%= form_for @availability_range  do |f| %>
    <label for="weekday"></label>
    <%= f.hidden_field(:weekday, value: x)%>
    <%= f.label :start_time %>
    <%= f.select(:start_time, AvailabilityRange::HOURS_OF_DAY) %>
    <%= f.label :end_time %>
    <%= f.select(:end_time, AvailabilityRange::HOURS_OF_DAY) %>
    <span class="actions">
    <%= f.submit "Add #{weekdays[x]} hours"%>
    </span>
    <% x = x + 1 %>
  <% end %>
<% end %>
